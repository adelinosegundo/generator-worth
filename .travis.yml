language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-worth' ]; then cd .. && eval "mv $currentfolder generator-worth" && cd generator-worth; fi

